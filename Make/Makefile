#CFLAGS+=-O1
#CXXFLAGS+=-O2
#LDFLAGS+=-static

TARGET1=main1
TARGET2=main2

TARGETS:=$(TARGET1)
TARGETS+=$(TARGET2)
all:compile $(TARGETS)

SOURCES:=testcpp.o
SOURCES+=testc.o
$(TARGETS):$(SOURCES)

TARGETSOBJ:=$(TARGET1).o
TARGETSOBJ+=$(TARGET2).o
compile:$(TARGETSOBJ)

clean:
	rm $(SOURCES) $(TARGETSOBJ) $(TARGETS)
